# Dependency directories (never commit this)
/node_modules/

# Local environment variables and secrets (never commit this)
.env
.env.local

# Logs
logs
*.log

# Ignore all text files (corrected syntax)
*.txt
